<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndIde" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/pro_header_footer</item>
        <item name="colorPrimaryVariant">@color/pro_on_surface</item>
        <item name="colorOnPrimary">@color/pro_on_surface</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pro_accent</item>
        <item name="colorSecondaryVariant">@color/pro_accent</item>
        <item name="colorOnSecondary">@color/pro_header_footer</item>

        <!-- Edge-to-Edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- UI Element Colors -->
        <item name="colorControlNormal">@color/pro_on_surface</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/pro_accent</item>
    </style>
</resources>